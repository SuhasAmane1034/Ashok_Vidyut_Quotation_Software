# ── QuoteFlow Server — Environment Variables ─────────────
# Copy this file to .env and edit values

# Port the Express server listens on
PORT=3001

# JWT secret — CHANGE THIS before deploying to production!
JWT_SECRET=quoteflow_secret_change_before_deploying

# SQLite database path (relative to server/)
DB_PATH=./quotations.db
